# Supabase Configuration (Required)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
SUPABASE_ANON_KEY=your-anon-key  # Optional, for token verification

# Extraction Backend (Optional)
# - ocr (default): Local OCR extraction, no external APIs
# - openai: Uses OpenAI API (requires OPENAI_API_KEY)
EXTRACTION_BACKEND=ocr

# Tesseract (Required if EXTRACTION_BACKEND=ocr and the tesseract binary is not in PATH)
# Common Windows path:
# TESSERACT_CMD=C:\Program Files\Tesseract-OCR\tesseract.exe
# If you installed from UB-Mannheim and used a different folder, point to that tesseract.exe.
# TESSERACT_CMD=

# OpenAI Configuration (Only required if EXTRACTION_BACKEND=openai)
# OPENAI_API_KEY=sk-your-openai-api-key
# OPENAI_MODEL=gpt-4o-mini  # Optional, defaults to gpt-4o-mini

# Google OAuth Configuration (Required for Google Sheets)
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret
REDIRECT_URI=http://localhost:3000/auth/callback

# Frontend URL
FRONTEND_URL=http://localhost:3000
